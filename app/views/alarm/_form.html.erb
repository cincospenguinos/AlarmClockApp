<%= form_for :alarm, :html => { id: 'new_alarm_form', :class => 'form-horizontal center'}, remote: true do |f| %>
    <div class="form-group">
      <%= f.label :name, 'Name', class: 'col-md-4 control-label' %>
      <div class="col-md-8">
        <%= f.text_field(:name, class: 'new_alarm', id: 'new_alarm_name') %>
      </div>
    </div>
    <div class="form-group">
      <%= f.label :time, 'Time', class: 'col-md-4 control-label' %>
      <div class="col-md-8">
        <%= f.time_select(:time) %>
      </div>
    </div>
    <div class="form-group">
      <%= f.label :days, 'Days', class: 'col-md-4 control-label' %>
      <div class="col-md-8">
        <div class="btn-group" data-toggle="buttons" id="new_alarm_days">
          <% Alarm.weekdays.each do |weekday| %>
              <label class="btn btn-xs btn-default new_alarm_day_checkbox">
                <%= weekday.capitalize %>
                <input type="checkbox" class="" name="days[]" value="<%= weekday.capitalize%>" />
              </label>
          <% end %>
        </div>
      </div>
    </div>
    <%= f.submit 'Submit', class: 'btn btn-default'%>
<% end %>